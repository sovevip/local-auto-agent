# CLAUDE.md — Local-Auto-Agent 项目导航图

> 你是谁，你在哪，哪些事绝对不能做，踩过什么坑。
> 新会话开始时读一次，不必每次任务都喂。

---

## 项目身份

**Local-Auto-Agent**：一个离线优先的电脑操作助手。云端大模型做任务规划，本地微调小模型（Qwen 7B）做工具执行，1+1 协同。

核心设计理念：**不让本地模型做推理，只让它做翻译**——把结构化 JSON 指令翻译成 `tools.py` 中的函数调用。

## 当前阶段

**Phase 2**：test_tools.py 全绿、分批测试工具选择率 92%（>80% 阈值）、跳过微调。工具库从 51 扩展到 68 个。

| 文件 | 用途 | 状态 |
|------|------|------|
| `Local-Auto-Agent-实施方案.md` | 全局规划书，6 个 Phase 路线图 | v1.3 |
| `tools.py` | 68 个安全收口的本地工具函数 | 已完成（含 Skill 动态加载） |
| `test_tools.py` | 工具函数自检 | 244+ 项全绿 |
| `test_fewshot.py` | 模型工具调用能力测试 | 结构完整 |
| `batch_fewshot_test.py` | 分批测试脚本 | 92% 工具选择率 |
| `skills/` | Skill 动态加载目录 | hello-skill 示例 |

## 架构决策

| 决策 | 原因 |
|------|------|
| 云端规划 + 本地执行 | 本地 7B 做翻译（92% 准确率），云端 DeepSeek 做多步规划 |
| Qwen2.5-7B-Instruct | 分批测试 92% 工具选择率达标，跳过微调 |
| 所有工具统一返回 `{"ok": bool, "data": any, "error": null|str}` | 消除返回格式不一致 |
| workspace 默认拒绝 | 不存在"忘记设置就全盘可访问" |
| execute_command 黑名单 | format/del/shutdown 等黑名单 + 确认 + 30s 超时 |

## 安全红线（不可协商）

1. **workspace 必须显式设置**。未调 `set_workspace()` 前，所有文件操作直接拒绝。
2. **写入和打开必须用户确认**。
3. **CMD 执行受黑名单保护**。
4. **模型不能绕过确认**。ask_user_confirmation 不暴露给模型 Schema。

## 避坑指南

- **不要用 `pyautogui` 坐标点击代替工具函数**
- **不要把项目背景和工具协议塞进同一个 Prompt**
- **用虚拟环境安装依赖**（`python -m venv .venv`）
- **test_tools.py 每工具独立用例**，不能循环统一参数
- **不要忘记注入确认 handler 再跑测试**

## 版本状态墙

| 日期 | 版本 | 事件 |
|------|------|------|
| 2026-05-19 | v1.0 | 初始方案 |
| 2026-05-19 | v1.1 | 安全收口 |
| 2026-05-19 | v1.2 | 默认拒绝 workspace |
| 2026-05-19 | v1.3 | 方案通过审核 |
| 2026-05-21 | v2.0 | 68 工具、Skill 系统、GitHub 集成、92% 工具选择率 |
